name: Cron Test

on:
  schedule:
    - cron: '* * * * *' # Every minute
  workflow_dispatch: # Allow manual trigger too

jobs:
  print_message:
    runs-on: ubuntu-latest
    steps:
    - name: Print test message
      run: echo "✅ GitHub Actions cron is working! Time is $(date -u)"
